# Documents

Place the **main file to analyze** here (`.txt` or `.pdf`) — for example, a news article about the election.

Set `DOCUMENT_PATH` in `.env` to the filename or path, e.g. `documents/my_article.txt`.

The default is `documents/example_article.txt`.
