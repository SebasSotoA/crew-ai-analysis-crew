# Corpus

Place **reference documents** here (`.txt` or `.pdf`). They are re-embedded on every run and used for semantic similarity against the main document in `documents/`.

Examples: older articles, candidate profiles, polling summaries, background explainers.
